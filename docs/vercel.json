{
  "buildCommand": "uv python install 3.12 && uv venv --python 3.12 .venv && uv pip install -r requirements.txt && .venv/bin/mkdocs build -f mkdocs.yml",
  "outputDirectory": "../site",
  "github": {
    "enabled": false
  }
}
